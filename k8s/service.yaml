apiVersion: v1
kind: Service
metadata:
  name: kubeassignment-api
  labels:
    app: kubeassignment-api
spec:
  type: LoadBalancer
  selector:
    app: kubeassignment-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
